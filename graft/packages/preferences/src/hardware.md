# packages/preferences/src/hardware.rs

- GpuInfo · struct · L24-L31 — pub struct GpuInfo
- CpuInfo · struct · L35-L38 — pub struct CpuInfo
- RamInfo · struct · L42-L44 — pub struct RamInfo
- HardwareReport · struct · L48-L52 — pub struct HardwareReport
- detect_sync · function · L62-L100 — pub fn detect_sync() -> Self
- detect · function · L106-L108 — pub async fn detect() -> Self
- detect_ram_mb · function · L114-L130 — fn detect_ram_mb() -> u64
- tests · module · L133-L152 — mod tests
- detection_ne_panique_jamais_et_renvoie_le_cpu · function · L137-L144 — fn detection_ne_panique_jamais_et_renvoie_le_cpu()
- ram_linux_est_lue_de_proc_meminfo · function · L148-L151 — fn ram_linux_est_lue_de_proc_meminfo()
